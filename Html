<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Flashcard</title>
    <style>
        .flashcard {
            width: 200px;
            height: 300px;
            margin: 50px auto;
            background-color: #f1f1f1;
            border: 2px solid #333;
            border-radius: 8px;
            display: flex;
            justify-content: center;
            align-items: center;
            font-family: Arial, sans-serif;
            font-size: 24px;
            cursor: pointer;
            text-align: center;
            transition: transform 0.3s;
        }

        .flashcard:hover {
            transform: scale(1.1);
        }

        .flashcard .front, .flashcard .back {
            position: absolute;
            width: 100%;
            height: 100%;
            display: flex;
            justify-content: center;
            align-items: center;
            backface-visibility: hidden;
        }

        .flashcard .back {
            transform: rotateY(180deg);
        }

        .flashcard-inner {
            position: relative;
            width: 100%;
            height: 100%;
            transform-style: preserve-3d;
            transform: rotateY(0deg);
            transition: transform 0.5s;
        }

        .flashcard:hover .flashcard-inner {
            transform: rotateY(180deg);
        }
    </style>
</head>
<body>

<div class="flashcard">
    <div class="flashcard-inner">
        <div class="front">
            <p>Pergunta: <br> Qual a capital do Brasil?</p>
        </div>
        <div class="back">
            <p>Resposta: <br> Brasília</p>
        </div>
    </div>
</div>

</body>
</html>
 
